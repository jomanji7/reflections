When would you want to use a remote repository rather than keeping all your work local?
when working remotely on my project.

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?
there might be some changes that wasn't ready to be public yet.

Describe the differences between forks, clones, and branches. When would you use one instead of another?
fork : clones repository inside github, no relation to the original
clone : clones repository to local from github or local, no relation to the original
branch : clones the project with a relation to the original , locally.
each one serves a different perpose.

What is the benefit of having a copy of the last known state of the remote stored locally?
to reduce the conflections when pushing to the remote.

How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?
with copying the content or sending them, or even with other softwares, but git and github is way easier.

When would you want to make changes in a separate branch rather than directly in master? What benefits does each approach have?
if there is a new feature or some stuff that can affect the way the project work, coz you dont want to make a version 
of your project that doesn't work public, but if there is a small changes that won't affect then it would be better 
done in the master branch.